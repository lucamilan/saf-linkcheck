# Iterations

The work bracket: what was **built and validated**. The body is **immutable** — fixes of
the same purpose are appended as dated amendments; a new surface earns a new iteration.

Form, fields and commands: `saf-tools describe --type iteration`.

# Decisions

The **why** behind a rule — what *binds* judgment. Append-only: a decision is never
modified or deleted, a new one **supersedes** it.

Form, fields and commands: `saf-tools describe --type decision`.

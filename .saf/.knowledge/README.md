# Knowledge

The reusable **lessons** distilled from reflection: heuristics, patterns, traps. It
*informs* judgment, where a decision *binds* it. Curated, not accumulated.

Form, fields and commands: `saf-tools describe --type knowledge`.
